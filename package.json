{
  "name": "japanese-anki-kards-generator",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"cd backend && python api.py\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
